name: Express Testing

run-name: Run Express Tests

on:
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    name: Deploy Express App
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        shell: pwsh
        run: |
          Write-Host "Hello!"
